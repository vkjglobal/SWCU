import { headers } from "next/headers";
import { db } from "@/lib/db";
import { requireStaffMembership } from "@/lib/authorise";
import { requireTenant } from "@/lib/tenant";
import { createStaffAccountAction, changeStaffRoleAction, setStaffActiveAction, reassignDraftAction, disableEditorWithReassignmentAction } from "@/app/admin/actions";
import { AdminShell, StatusBadge } from "@/app/admin/admin-shell";
import { StaffResetInitiation } from "@/components/staff-reset-initiation";

export default async function StaffPage() {
  const tenant = await requireTenant((await headers()).get("host") ?? "");
  const { membership } = await requireStaffMembership(tenant, ["ADMINISTRATOR"]);
  const staff = await db.staffMembership.findMany({ where: { tenantId: tenant.id }, include: { user: { select: { name: true, email: true } } }, orderBy: [{ isActive: "desc" }, { createdAt: "asc" }] });
  const editors = staff.filter((item) => item.role === "EDITOR" && item.isActive);
   const disabledEditorIds = staff.filter((item) => item.role === "EDITOR" && !item.isActive).map((item) => item.userId);
   const openDrafts = await db.cmsDraft.findMany({ where: { tenantId: tenant.id, status: { in: ["DRAFT", "WAITING_FOR_APPROVAL", "RETURNED_FOR_CHANGES"] }, OR: [{ assignedTo: null }, { assignedTo: { in: disabledEditorIds } }] }, select: { id: true, kind: true, createdBy: true, assignedTo: true } });
  return <AdminShell role={membership.role} title="Staff accounts" intro="Manage who can prepare and approve public content. Passwords are never shown here.">
      <div className="mt-8 grid gap-6 lg:grid-cols-[1fr_1.5fr]"><section className="rounded-card border border-deep-navy/10 bg-white p-6 shadow-card"><h2 className="font-heading text-xl font-bold text-deep-navy">Add a staff account</h2><p className="mt-2 text-sm text-charcoal/65">Use a temporary setup password. The staff member can sign in and change it through the supported account process.</p><form action={createStaffAccountAction} className="mt-5 space-y-4"><label className="block text-sm font-semibold">Name<input name="name" required className="mt-1 w-full rounded-lg border border-deep-navy/15 px-3 py-2"/></label><label className="block text-sm font-semibold">Email<input name="email" type="email" required className="mt-1 w-full rounded-lg border border-deep-navy/15 px-3 py-2"/></label><label className="block text-sm font-semibold">Temporary setup password<input name="password" type="password" minLength={8} required className="mt-1 w-full rounded-lg border border-deep-navy/15 px-3 py-2"/></label><label className="block text-sm font-semibold">Role<select name="role" className="mt-1 w-full rounded-lg border border-deep-navy/15 px-3 py-2"><option value="EDITOR">Editor</option><option value="ADMINISTRATOR">Administrator</option></select></label><button className="button-primary" type="submit">Create account</button></form><p className="mt-4 text-sm text-charcoal/60">For an existing staff member, an Administrator can start the supported password reset process below. No passwords are displayed here.</p></section><section className="rounded-card border border-deep-navy/10 bg-white p-6 shadow-card"><h2 className="font-heading text-xl font-bold text-deep-navy">Current staff</h2><div className="mt-4 space-y-3">{staff.map((item) => { const needsReassignment = item.role === "EDITOR" && item.isActive && openDrafts.some((draft) => draft.createdBy === item.userId || draft.assignedTo === item.userId); return <div key={item.id} className="rounded-lg border border-deep-navy/10 p-4"><div className="flex flex-wrap items-center justify-between gap-3"><div><p className="font-semibold text-deep-navy">{item.user.name}</p><p className="text-sm text-charcoal/65">{item.user.email}</p></div><StatusBadge status={item.isActive ? "ACTIVE" : "DISABLED"}/></div><div className="mt-3 flex flex-wrap gap-2"><StaffResetInitiation membershipId={item.id}/><form action={changeStaffRoleAction}><input type="hidden" name="membershipId" value={item.id}/><select name="role" defaultValue={item.role} aria-label={`Role for ${item.user.name}`} className="rounded-lg border border-deep-navy/15 px-2 py-1 text-sm"><option value="EDITOR">Editor</option><option value="ADMINISTRATOR">Administrator</option></select><button className="ml-2 rounded-lg border border-swcu-blue/30 px-3 py-1 text-sm font-semibold text-swcu-blue" type="submit">Save role</button></form>{needsReassignment ? <form action={disableEditorWithReassignmentAction} className="flex flex-wrap gap-2"><input type="hidden" name="membershipId" value={item.id}/><select name="assigneeUserId" required aria-label={`Replacement Editor for ${item.user.name}`} className="rounded-lg border border-deep-navy/15 px-2 py-1 text-sm"><option value="">Choose replacement Editor</option>{editors.filter((editor) => editor.userId !== item.userId).map((editor) => <option key={editor.userId} value={editor.userId}>{editor.user.name}</option>)}</select><button className="rounded-lg border border-swcu-red/30 px-3 py-1 text-sm font-semibold text-swcu-red" type="submit">Reassign and disable</button></form> : <form action={setStaffActiveAction}><input type="hidden" name="membershipId" value={item.id}/><input type="hidden" name="isActive" value={item.isActive ? "false" : "true"}/><button className="rounded-lg border border-deep-navy/15 px-3 py-1 text-sm font-semibold text-charcoal" type="submit">{item.isActive ? "Disable access" : "Reactivate"}</button></form>}</div></div>; })}</div></section></div>
    {openDrafts.length > 0 && <section className="mt-6 rounded-card border border-deep-navy/10 bg-white p-6 shadow-card"><h2 className="font-heading text-xl font-bold text-deep-navy">Reassign open drafts</h2><p className="mt-2 text-sm text-charcoal/65">These drafts need an active Editor before they can continue.</p><div className="mt-4 space-y-3">{openDrafts.map((draft) => <form key={draft.id} action={reassignDraftAction} className="flex flex-wrap items-center gap-3 rounded-lg bg-soft-blue-grey p-3"><span className="text-sm font-semibold text-deep-navy">{draft.kind.replaceAll("_", " ")}</span><input type="hidden" name="draftId" value={draft.id}/><select name="assigneeUserId" required className="rounded-lg border border-deep-navy/15 px-3 py-2 text-sm"><option value="">Choose an active Editor</option>{editors.map((editor) => <option key={editor.userId} value={editor.userId}>{editor.user.name}</option>)}</select><button className="button-secondary" type="submit">Reassign</button></form>)}</div></section>}
  </AdminShell>;
}